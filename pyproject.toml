[project]
name = "hockey-stats"
version = "0.3.0"
description = "Django application for managing amateur hockey stats (Clean Architecture, Python + SQLite)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Django==5.2.13",
]
